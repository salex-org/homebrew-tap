# Homebrew Tap

This repository contains software provided by salex-org for installation using homebrew.

Add the Tap to homebrew:

```shell
brew tap salex-org/homebrew-tap
```

Search content of the Tap:

```shell
brew search salex-org/tap
```
